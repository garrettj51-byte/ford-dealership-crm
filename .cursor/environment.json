{
  "name": "Ford Dealership CRM",
  "install": "npm ci && cp --update=none .env.example .env && npx prisma migrate deploy && npx prisma db seed",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Next.js dev server (Turbopack) on http://localhost:3000"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
